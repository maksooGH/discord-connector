# Discord bot token — Developer Portal > Bot > Reset Token
DISCORD_BOT_TOKEN=

# Where the SQLite capture DB lives (created on first run)
DISCORD_DB_PATH=./data/discord.db

# Optional: Google service-account JSON for the sheets helpers.
# Leave unset if you are not using core.sheets
GOOGLE_SERVICE_ACCOUNT=
